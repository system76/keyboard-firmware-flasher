#!/usr/bin/env bash

# This is a very simple script to pull the most recent version of this script from git
# It should do nothing else, that way commit versions can be uptated in ./main.sh and
# be automatically updated without intervention
#git pull origin

./src/main.sh
exit
